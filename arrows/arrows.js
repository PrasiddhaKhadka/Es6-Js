// We are certain to write ES5 Arrow functions before

const add =  (a, b) => {
    return a + b;
}

const square = x => x * x;

// Example of an arrow function for ES6

const multiply = (a) => 'Multiplying! ' + (a * 2);


// Example of an arrow function with single parameter

const greet = name => 'Hello, ' + name + '!';

greet('World'); // "Hello, World!"


